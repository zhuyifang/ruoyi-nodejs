// src/{{moduleName}}/{{moduleName}}.module.ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
// 【修复】使用 modulePrefix 统一导入
import { {{modulePrefix}}Controller } from './{{moduleName}}.controller';
import { {{modulePrefix}}Service } from './{{moduleName}}.service';
// 【保持】实体类名仍然使用 entityName
import { {{entityName}} } from './{{moduleName}}.entity';

@Module({
    imports: [TypeOrmModule.forFeature([{{entityName}}])],
    // 【修复】使用 modulePrefix 统一引用
    controllers: [{{modulePrefix}}Controller],
    providers: [{{modulePrefix}}Service],
    exports: [{{modulePrefix}}Service],
})
// 【修复】移除硬编码的 'Plugin' 前缀
export class {{modulePrefix}}Module {}